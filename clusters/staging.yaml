---

apiVersion: devopstoolkitseries.com/v1alpha1
kind: ClusterClaim
metadata:
  name: staging
  namespace: clusters
spec:
  id: staging
  compositionSelector:
    matchLabels:
      provider: aws
      cluster: eks
  parameters:
    nodeSize: medium
    minNodeCount: 2
  writeConnectionSecretToRef:
    name: cluster-staging
